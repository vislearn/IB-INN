[checkpoints]
base_name = ablation_beta0_mnist
interval_log = 240

[data]
dataset = MNIST
label_smoothing = 0.01

[model]
act_norm = 0.80

[training]
beta_IB = 0.
n_epochs = 200
scheduler_milestones = [100, 160]
