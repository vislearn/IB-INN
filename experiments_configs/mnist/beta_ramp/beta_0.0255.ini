[checkpoints]
base_name = beta_0.0255_mnist
interval_checkpoint = 200
interval_figure = 200
interval_log = 240

[training]
beta_IB = 0.0255
n_epochs = 60
scheduler_milestones = [50]

[data]
dataset = MNIST
label_smoothing = 0.01

[model]
act_norm = 0.80
